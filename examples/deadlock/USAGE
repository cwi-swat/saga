1. Generate aspect and parser for attribute grammar, then compile all sources:
   mvn clean package
2. Start the program with assertions enabled (-ea) and antlr and aspectj in the classpath. The program executes until a deadlock is detected:
   java -cp target/lib/antlr-runtime-3.4.jar:target/lib/aspectjrt-1.7.3.jar:target/lib/jgrapht-core-0.9.1.jar:target/deadlock-1.0-SNAPSHOT.jar -ea nl.cwi.saga.deadlock.Deadlock
